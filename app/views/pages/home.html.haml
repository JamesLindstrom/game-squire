%h1 Greetings, Game Master!

%p Game Squire is the perfect tool to help you run games online or in person.

- if current_user
  - if @game_spaces.any?
    %p Select a Game Space
    %ul
      - @game_spaces.each do |game_space|
        %li= link_to "#{game_space.name}#{' (LIVE)' if game_space.public}", game_space_path(game_space)
  = link_to 'Create a Game Space', new_game_space_path, class: 'button'
  = link_to 'Manage Creatures', creatures_path, class: 'button'
- else
  %p
    = link_to 'Login', new_user_session_path
    or
    = link_to 'Signup', new_user_registration_path
    to get started.
