.field-holder
  = f.hidden_field :game_space_id

  .field
    = f.label :name
    = f.text_field :name, required: true
    = render_error_messages @encounter, :name

  .field
    = f.label 'Creatures'
    = f.grouped_collection_select :creature_ids, grouped_creatures, :creatures, :name, :id, :name, {}, { multiple: true }
