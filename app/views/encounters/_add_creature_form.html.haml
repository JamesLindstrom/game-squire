= form_with url: add_creature_encounter_path(@encounter), method: :patch, remote: true do |f|
  .field-holder
    .field
      = f.label 'Select a Creature'
      = f.collection_select :creature_id, @creatures, :id, :name

  = f.submit 'Add Creature', class: 'button'
